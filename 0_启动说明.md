# AI 健身系统 — 启动说明

## 快速启动（3 步）

**1. 装依赖（项目根目录执行一次）**
```bash
pip install flask opencv-python mediapipe numpy
```

**2. 启动后端**
```bash
python flaskrun.py
```
看到 `Running on http://0.0.0.0:90` 即成功。`tmp` 目录会自动创建，无需手建。

**3. 启动小程序**
- 用 **微信开发者工具** 打开目录：`miniprogram`
- 选「使用测试号」或「不使用 AppID」即可本地跑
- 点击「编译」在模拟器里用

---

## 小程序要连本机后端时

模拟器/本机调试时，小程序里请求的地址用 **`http://127.0.0.1:90`** 即可（后端已改为 `0.0.0.0`，本机可访问）。

若当前代码里是别的 IP（如 `www.farvuav.cc:90`），改成 `127.0.0.1:90`：

- `miniprogram/pages/sport/sport.js`：`/upload`、`/detect` 的 url
- `miniprogram/pages/sportTwo/sportTwo.js`：`/upload`、`/situp` 的 url

**真机预览**：手机和电脑同一 WiFi，把上面地址改成你电脑的局域网 IP（如 `http://192.168.1.100:90`），在 `flaskrun.py` 里不用改。

---

## 提示「请先登录！」时怎么登录？

点底部 **「我的」** tab → 在「点击头像登录」或头像区域点一下 → 按提示完成微信授权即可。登录后再用首页的「俯卧撑」「仰卧起坐」「全部记录」就不会再提示。

---

## 遇到问题

| 情况 | 处理 |
|------|------|
| 小程序请求失败 | 先确认终端里 Flask 已启动且无报错；真机必须用电脑 IP，不能用 127.0.0.1 |
| 依赖装得慢 | 用国内镜像：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple flask opencv-python mediapipe numpy` |
| 云开发/历史记录 | 可选；要用时再在微信公众平台注册小程序、配置 AppID 并部署云函数 |

总结：**装依赖 → 运行 `python flaskrun.py` → 用微信开发者工具打开 `miniprogram` 并编译**，即可跑起来。
